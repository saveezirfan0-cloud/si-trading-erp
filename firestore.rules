rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow all authenticated users to read/write everything
    // Tighten this once you have multiple staff users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
